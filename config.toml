# Use this repository as CODEX_HOME to load the versioned global AGENTS.md
# and each active repository's AGENT_LOCAL.md specialization or fallback automatically.
project_doc_fallback_filenames = ["AGENT_LOCAL.md"]
